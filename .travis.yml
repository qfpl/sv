# This Travis job script has been generated by a script via
#
#   haskell-ci 'cabal.project'
#
# For more information, see https://github.com/haskell-CI/haskell-ci
#
# version: 0.3
#
language: c
dist: xenial

git:
  submodules: false  # whether to recursively clone submodules

cache:
  directories:
    - $HOME/.cabal/packages
    - $HOME/.cabal/store

before_cache:
  - rm -fv $CABALHOME/packages/hackage.haskell.org/build-reports.log
  # remove files that are regenerated by 'cabal update'
  - rm -fv $CABALHOME/packages/hackage.haskell.org/00-index.*
  - rm -fv $CABALHOME/packages/hackage.haskell.org/*.json
  - rm -fv $CABALHOME/packages/hackage.haskell.org/01-index.cache
  - rm -fv $CABALHOME/packages/hackage.haskell.org/01-index.tar
  - rm -fv $CABALHOME/packages/hackage.haskell.org/01-index.tar.idx

  - rm -rfv $CABALHOME/packages/head.hackage

matrix:
  include:
    - compiler: "ghc-8.6.4"
      addons: {apt: {packages: [ghc-ppa-tools,cabal-install-2.4,ghc-8.6.4], sources: [hvr-ghc]}}
    - compiler: "ghc-8.4.4"
      addons: {apt: {packages: [ghc-ppa-tools,cabal-install-2.4,ghc-8.4.4], sources: [hvr-ghc]}}
    - compiler: "ghc-8.2.2"
      addons: {apt: {packages: [ghc-ppa-tools,cabal-install-2.4,ghc-8.2.2], sources: [hvr-ghc]}}
    - compiler: "ghc-8.0.2"
      addons: {apt: {packages: [ghc-ppa-tools,cabal-install-2.4,ghc-8.0.2], sources: [hvr-ghc]}}
    - compiler: "ghc-7.10.3"
      addons: {apt: {packages: [ghc-ppa-tools,cabal-install-2.4,ghc-7.10.3], sources: [hvr-ghc]}}

before_install:
  - HC=/opt/ghc/bin/${CC}
  - HCPKG=${HC/ghc/ghc-pkg}
  - unset CC
  - CABAL=/opt/ghc/bin/cabal
  - CABALHOME=$HOME/.cabal
  - export PATH="$CABALHOME/bin:$PATH"
  - ROOTDIR=$(pwd)
  - HCNUMVER=$(( $(${HC} --numeric-version|sed -E 's/([0-9]+)\.([0-9]+)\.([0-9]+).*/\1 * 10000 + \2 * 100 + \3/') ))
  - echo $HCNUMVER

install:
  - ${CABAL} --version
  - echo "$(${HC} --version) [$(${HC} --print-project-git-commit-id 2> /dev/null || echo '?')]"
  - TEST=--enable-tests
  - BENCH=--enable-benchmarks
  - GHCHEAD=${GHCHEAD-false}
  - travis_retry ${CABAL} update -v
  - sed -i.bak 's/^jobs:/-- jobs:/' $CABALHOME/config
  - rm -fv cabal.project cabal.project.local
  - grep -Ev -- '^\s*--' $CABALHOME/config | grep -Ev '^\s*$'
  - rm -f cabal.project
  - touch cabal.project
  - "printf 'packages: \"./sv\"\\n' >> cabal.project"
  - "printf 'packages: \"./benchmarks\"\\n' >> cabal.project"
  - "printf 'packages: \"./examples\"\\n' >> cabal.project"
  - "printf 'packages: \"./sv-cassava\"\\n' >> cabal.project"
  - "printf 'packages: \"./sv-core\"\\n' >> cabal.project"
  - "printf 'packages: \"./sv-svfactor\"\\n' >> cabal.project"
  - "printf 'packages: \"./weigh\"\\n' >> cabal.project"
  - "echo 'optimization: 2' >> cabal.project "
  - "printf 'write-ghc-environment-files: always\\n' >> cabal.project"
  - touch cabal.project.local
  - "for pkg in $($HCPKG list --simple-output); do echo $pkg | sed 's/-[^-]*$//' | grep -vE -- '^(sv|sv-benchmarks|sv-cassava|sv-core|sv-examples|sv-svfactor|sv-weigh)$' | sed 's/^/constraints: /' | sed 's/$/ installed/' >> cabal.project.local; done"
  - cat cabal.project || true
  - cat cabal.project.local || true
  - if [ -f "./sv/configure.ac" ]; then (cd "./sv" && autoreconf -i); fi
  - if [ -f "./benchmarks/configure.ac" ]; then (cd "./benchmarks" && autoreconf -i); fi
  - if [ -f "./examples/configure.ac" ]; then (cd "./examples" && autoreconf -i); fi
  - if [ -f "./sv-cassava/configure.ac" ]; then (cd "./sv-cassava" && autoreconf -i); fi
  - if [ -f "./sv-core/configure.ac" ]; then (cd "./sv-core" && autoreconf -i); fi
  - if [ -f "./sv-svfactor/configure.ac" ]; then (cd "./sv-svfactor" && autoreconf -i); fi
  - if [ -f "./weigh/configure.ac" ]; then (cd "./weigh" && autoreconf -i); fi
  - rm -f cabal.project.freeze
  - ${CABAL} new-freeze -w ${HC} ${TEST} ${BENCH} --project-file="cabal.project" --dry
  - "cat \"cabal.project.freeze\" | sed -E 's/^(constraints: *| *)//' | sed 's/any.//'"
  - rm  "cabal.project.freeze"
  - ${CABAL} new-build -w ${HC} ${TEST} ${BENCH} --project-file="cabal.project" --dep -j2 all
  - ${CABAL} new-build -w ${HC} --disable-tests --disable-benchmarks --project-file="cabal.project" --dep -j2 all
  - rm -rf .ghc.environment.* "./sv"/dist "./benchmarks"/dist "./examples"/dist "./sv-cassava"/dist "./sv-core"/dist "./sv-svfactor"/dist "./weigh"/dist
  - DISTDIR=$(mktemp -d /tmp/dist-test.XXXX)

# Here starts the actual work to be performed for the package under test;
# any command which exits with a non-zero exit code causes the build to fail.
script:
  # test that source-distributions can be generated
  - ${CABAL} new-sdist all
  - mv dist-newstyle/sdist/*.tar.gz ${DISTDIR}/
  - cd ${DISTDIR} || false
  - find . -maxdepth 1 -name '*.tar.gz' -exec tar -xvf '{}' \;
  - rm -f cabal.project
  - touch cabal.project
  - "printf 'packages: \"sv-*/*.cabal\"\\n' >> cabal.project"
  - "printf 'packages: \"sv-benchmarks-*/*.cabal\"\\n' >> cabal.project"
  - "printf 'packages: \"sv-examples-*/*.cabal\"\\n' >> cabal.project"
  - "printf 'packages: \"sv-cassava-*/*.cabal\"\\n' >> cabal.project"
  - "printf 'packages: \"sv-core-*/*.cabal\"\\n' >> cabal.project"
  - "printf 'packages: \"sv-svfactor-*/*.cabal\"\\n' >> cabal.project"
  - "printf 'packages: \"sv-weigh-*/*.cabal\"\\n' >> cabal.project"
  - "echo 'optimization: 2' >> cabal.project "
  - "printf 'write-ghc-environment-files: always\\n' >> cabal.project"
  - touch cabal.project.local
  - "for pkg in $($HCPKG list --simple-output); do echo $pkg | sed 's/-[^-]*$//' | grep -vE -- '^(sv|sv-benchmarks|sv-cassava|sv-core|sv-examples|sv-svfactor|sv-weigh)$' | sed 's/^/constraints: /' | sed 's/$/ installed/' >> cabal.project.local; done"
  - cat cabal.project || true
  - cat cabal.project.local || true
  # this builds all libraries and executables (without tests/benchmarks)
  - ${CABAL} new-build -w ${HC} --disable-tests --disable-benchmarks all

  # build & run tests, build benchmarks
  - ${CABAL} new-build -w ${HC} ${TEST} ${BENCH} all
  - ${CABAL} new-test -w ${HC} ${TEST} ${BENCH} all

  # cabal check
  - (cd sv-* && ${CABAL} check)
  - (cd sv-benchmarks-* && ${CABAL} check)
  - (cd sv-examples-* && ${CABAL} check)
  - (cd sv-cassava-* && ${CABAL} check)
  - (cd sv-core-* && ${CABAL} check)
  - (cd sv-svfactor-* && ${CABAL} check)
  - (cd sv-weigh-* && ${CABAL} check)

  # haddock
  - ${CABAL} new-haddock -w ${HC} ${TEST} ${BENCH} all

  # Build without installed constraints for packages in global-db
  - rm -f cabal.project.local; ${CABAL} new-build -w ${HC} --disable-tests --disable-benchmarks all

# REGENDATA ["cabal.project"]
# EOF
